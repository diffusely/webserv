# Пока не парсится кодом (см. Config.hpp) — это заготовка формата
# для Дня 10 roadmap, чтобы структура была видна заранее.

server {
	listen 8080;
	server_name localhost;
	root ./www;
	index index.html;

	location / {
		methods GET POST DELETE;
		autoindex off;
	}
}
